interface Circle_data{
    start_r: number,
    ring_num: number,
    dist: number,
    circle_r: number,
    circle_num: number[],
    circle_color: string,
}

interface Com_data{
    center_x:number,
    center_y:number,
    start_angle:number,
}

export type {Circle_data,Com_data};